;; Isn't it crazy that `def` itself isn't built in?
;; But then if the `:env` channel isn't hooked up correctly, you can't define
;; `def` at all, which is a pretty hardcore form of sandboxing...
(emit :env [def ~(μ x ~(emit :env [~(nth* ~x 1) ~~(nth* ~x 2)] :return ~(nth* ~x 1)))]
      :return def)

;; REVIEW: This initial def cannot accept metadata like docstrings, file name,
;; line & col numbers, etc. So we'll need to use it to write a better one
;; eventually.

(def if* ; previously `select`.
  (μ x
     ~(nth* [~(nth* ~x 2) ~(nth* ~x 3)] ~(-* 2 ~(get* {false 0 true 1} ~(nth* ~x 1))))))

(def if
  (μ x
     ~~(if* ~~(nth* ~x 1) ~(nth* ~x 2) ~(nth* ~x 3))))

;; "Wraps a μ or primitive that takes only literal values in a function that
;; evals its arguments."
(def wrap
  (μ f
     ~(μ args
         ~(~f . ~~args))))

(def nth (wrap . nth*))

(def count (wrap . count*))

(def first (wrap . first*))
(def rest (wrap . rest*))

(def + (wrap . +*))
(def - (wrap . -*))
(def * (wrap . **))
(def / (wrap . /*))

(def > (wrap . >*))
(def < (wrap . <*))
(def = (wrap . =*))

(def not (wrap . not*))

(def symbol? (wrap . symbol?*))
(def empty? (wrap . empty?*))
(def map? (wrap . map?*))
(def list? (wrap . list?*))
(def merge (wrap . merge*))

(def str (wrap . str*))

(def mod (wrap . mod*))

(def even? (wrap . (μ x ~(=* 0 ~(mod* ~(nth* ~x 1) 2)))))

;; REVIEW: Is there any point wrapping this?
(def odd? (wrap . (μ x ~(not* ~(even? . x)))))

;;;;; Core

;;; bootstrap destructuring

(def cond
  (μ cond body
     ~(if (<* ~(count* ~body) 2)
        (emit)
        (if ~(nth* ~body 1)
          ~(nth* ~body 2)
          (cond . ~(rest* ~(rest* ~body)))))))

(def let1
  (μ x
     ~((μ ~(nth* ~x 1) ~(nth* ~x 3)) . ~(nth* ~x 2))))

(def let*
  (μ let* x
     ~(let1 bindings ~(nth* ~x 1)
            ~(let1 body ~(nth* ~x 2)
                   ~(if (empty?* ~bindings)
                      body
                      (let1 ~(nth* ~bindings 1) ~(nth* ~bindings 2)
                            ~(let* ~(rest* ~(rest* ~bindings)) ~body)))))))

(def let-map
  (μ let-map x
     ~(let1 bindings ~(nth* ~x 1)
            ~(let1 body ~(nth* ~x 2)
                   ~(if* ~(empty?* ~bindings)
                      ~body
                      ~(let1 kv ~(first* ~bindings)
                             ~(let1 ~(nth* ~kv 1) ~(nth* ~kv 2)
                                    ~(let-map ~(rest* ~bindings) ~body))))))))

(def destructure-map
  (μ mp
     ~(emit :error "not implemented")))

(def destructure
  (μ destructure xy
     ~(let* [params ~(nth* ~xy 1)
             args   ~(nth* ~xy 2)]
        ~(cond
           ;; TODO: Bind rest ( . ). Probably need reader support.
           (symbol?* ~params) {params args}
           (empty?* ~params)  {}
           (empty?* ~args)    (emit :error "pattern mismatch")
           (list?* ~params)   (merge
                               (destructure ~(first* ~params) ~(first* ~args))
                               (destructure ~(rest* ~params) ~(rest* ~args)))
           (map?* ~params)    (merge
                               (destructure-map ~(first* ~params) ~(first* ~args))
                               (destructure ~(rest* ~params) ~(rest* ~args)))
           true               (emit :error (str "Cannot destructure: " params ", " args))))))


;;; definition constructs

(def defμ
  (μ x
     ~(def ~(nth* ~x 1)
        (μ ~(nth* ~x 1) args
           ~(let-map ~(destructure ~(nth* ~x 2) ~args)
              ~~(nth* ~x 3))))))

;; TODO: metadata on def (source file, line, col, docstring, arbitrary extras))

;; TODO: Overloading: at least multiple arities. Better would be overloading by
;; destructuring signature, but we'll need to be able to detect ambiguity and
;; have a deterministic selection. Julia's type matching is great, but it's a
;; sat solver under then hood which is why the compiler blows up sometimes on
;; bigger codebases, which is a hell of a downside.

(defμ fn2 [params body]
  (μ args
     ~(let-map ~(destructure ~params ~~args)
        ~~body)))

(defμ fn3 [name params body]
  ~(μ ~name args
      ~(let-map ~(destructure ~params ~~args)
         ~~body)))

(defμ fn args
  (let1 indirect ~(nth* ~[fn2 fn3] ~(-* ~(count* ~args) 1))
        ~(indirect . ~args)))

(defμ defn x
  (def ~(nth* ~x 1) (fn . ~x)))

(defμ when [p t]
  (if ~p ~t (emit)))

(defn inc [x]
  (+ 1 x))

(defn ! [n]
  (if (< n 2)
    1
    (* n (! (- n 1)))))
