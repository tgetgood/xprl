(+ 1 4) ; => 5

((μ x ~(+ ~(nth* ~x 1) 1)) 4) ; => 5

((μ y ~((μ x ~(+ ~(nth* ~x 1) 1)) ~(nth* ~y 1))) 4) ; => 5

((μ y ~((μ z ~(+ (nth z 1) 1)) ~(nth* ~y 1))) 4) ; => 5

((μ y ~((μ x ~(+ (nth x 1) 1)) ~(nth* ~y 1))) 4)
;; => ClassCastException ast.Symbol is not a java.lang.Number
;; As the previous example shows this is a scoping issue. Why do name conflicts
;; prevent variables from being resolved? Presumably they resolve to the wrong
;; value.
